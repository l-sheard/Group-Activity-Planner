// Template — safe to commit. After cloning, copy this to `config.js`
// and replace each value with your own Firebase project config.
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_AUTH_DOMAIN",
  projectId: "PASTE_PROJECT_ID",
  storageBucket: "PASTE_STORAGE_BUCKET",
  messagingSenderId: "PASTE_SENDER_ID",
  appId: "PASTE_APP_ID",
};

// Optional: live-subscription Cloudflare Worker URL (see README).
// Leave empty until you've deployed cloudflare-worker.js.
window.WORKER_URL = "";
